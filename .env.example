# -----------------------------------------------------------------------
# Stock Analyst Agent – environment variable template
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
# -----------------------------------------------------------------------

# --- MCP Server --------------------------------------------------------
# URL of the live NSE/BSE MCP server.
# Default: https://lobehub.com/mcp/girishkumardv-live-nse-bse-mcp
MCP_URL=https://lobehub.com/mcp/girishkumardv-live-nse-bse-mcp

# API key for the MCP server (if required by your deployment).
# Leave blank if the server does not require authentication.
MCP_API_KEY=

# --- Ollama (local LLM) ------------------------------------------------
# Base URL of your local Ollama instance.
OLLAMA_URL=http://localhost:11434

# Model to use with Ollama (e.g. llama3, mistral, phi3).
OLLAMA_MODEL=llama3

# --- Google Gemini -----------------------------------------------------
# Your Google AI Studio / Vertex AI API key.
# Get one at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Gemini model name.
GEMINI_MODEL=gemini-1.5-flash

# --- Caching -----------------------------------------------------------
# Cache TTL in seconds (default: 300 = 5 minutes).
CACHE_TTL_SECONDS=300

# --- Logging -----------------------------------------------------------
# Python logging level: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO
